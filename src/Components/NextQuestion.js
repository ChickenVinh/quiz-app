import React from 'react'
import {Button, Col} from 'react-bootstrap'

const NextQuestion = () => {
    return(
        <Col className = "mt-3 d-flex justify-content-center"><Button variant = 'primary'>Next</Button></Col>
    )
}

export default NextQuestion